# セキュリティポリシー

## サポート対象バージョン

本プロジェクトは **Phase 0(スキャフォールドのみ / 未稼働)** であり、まだリリース版が存在しません。現時点でセキュリティ修正の対象となるのは `main` ブランチのみです。v0.1.0 のリリース(ロードマップ Phase 4)以降、サポート対象バージョンをこの表に記載します。

| バージョン | サポート状況 |
|---|---|
| `main` (Phase 0) | 修正対象 |

## 脆弱性の報告方法

**脆弱性を公開 Issue、Pull Request、Discussion、SNS で報告しないでください。**

報告には **GitHub Security Advisories** の非公開報告機能を使ってください。

1. 本リポジトリの **Security** タブを開く
2. **Report a vulnerability** を選択する
3. フォームに詳細を記入して送信する

これにより、修正が用意できるまで内容が非公開に保たれた状態で、メンテナと安全にやり取りできます。

GitHub アカウントをお持ちでない場合など、上記の手段が使えないときは `nomhiro1204@gmail.com` までご連絡ください。

**公開 Issue で連絡先を尋ねることはしないでください。**その時点で脆弱性の存在が公開されてしまいます。

### 報告に含めていただきたい情報

- 影響を受けるコンポーネント(Core API / MCP Server / Fuseki 構成 / IaC など)と該当箇所
- 再現手順、または概念実証(PoC)
- 想定される影響(情報漏洩、権限昇格、サービス停止など)
- 発見に使用した環境・バージョン・設定

### 対応の流れと目安

| 段階 | 目安 |
|---|---|
| 受領の確認 | 5 営業日以内 |
| 初期評価(影響範囲と深刻度の判断)の共有 | 10 営業日以内 |
| 修正と公開 Advisory の発行 | 深刻度に応じて協議 |

本プロジェクトは個人が運営する OSS であり、24 時間体制の対応体制は持っていません。上記は努力目標であり、SLA ではありません。

### 開示方針

修正の準備が整ってから公開する **coordinated disclosure** を原則とします。修正版の公開に合わせて GitHub Security Advisory を発行し、報告者のクレジットを記載します(希望しない場合はお知らせください)。バグバウンティ(報奨金)制度は用意していません。

---

## このプロジェクト固有の攻撃面

本プロジェクトは SPARQL エンドポイントと LLM を扱うため、一般的な Web アプリとは異なる攻撃面があります。設計上の対策と、報告をとくに歓迎する領域を以下に整理します。

### SPARQL `SERVICE` 句による SSRF

SPARQL 1.1 の `SERVICE` 句は、クエリの実行中にサーバーから**任意の外部エンドポイントへ HTTP リクエストを発行させることができます**。これは仕様どおりの機能ですが、そのまま公開すると典型的な SSRF(Server-Side Request Forgery)になります。とくに Azure 環境では、Azure Instance Metadata Service(`169.254.169.254`)への到達を許すと、Managed Identity のアクセストークンを取得される恐れがあります。

**設計上の対策**:

- 外部連邦(federation)先を**既定で禁止**し、必要な場合のみ allowlist 方式で明示的に許可する
- リンクローカルアドレス(`169.254.0.0/16`)、ループバック、VNet 内部アドレスへの到達を遮断する
- Fuseki / Ontop は **internal ingress のみ**で公開し、インターネットから直接到達できないようにする

### クエリによるサービス拒否(DoS)

SPARQL は表現力が高く、意図せずとも組み合わせ爆発を起こすクエリ(制約のない複数ホップのパス、大量の直積を生む結合、`OPTIONAL` の深い入れ子など)を容易に書けます。単一のクエリでストアの CPU とメモリを飽和させられます。

**設計上の対策**:

- クエリタイムアウトの強制
- 結果件数の上限
- Fuseki は読み取り専用の内部サービスとして分離し、API の可用性から切り離す

### 書き込み経路の限定

エージェント・Web・外部クライアントに提供するのは**読み取りのみ**です。SPARQL Update および Graph Store Protocol による書き込みは、内部エンドポイント経由で **Core API のみ**が行います。変更は必ず「正本(PostgreSQL + Blob)に書く → ストアへ射影」の順序で実行されます。

この設計により、仮に読み取り経路が悪用されてもグラフの内容を改変できません。逆に言えば、**Fuseki に直接書き込まれた内容は再構築時に失われます**(これは設計上の制約であり、バグではありません)。詳細は [ADR-0002](docs/adr/0002-triple-store-as-rebuildable-projection.md) を参照してください。

### 名前空間の越境

マルチテナント的な分離は**名前空間ごとの Fuseki データセット**で実現し、任意 SPARQL のクエリ書き換えによる分離は行いません(書き換えによる分離は迂回されやすいためです)。名前空間をまたぐクエリは拒否します。名前空間 × ロールの権限は PostgreSQL で管理し、API で強制します。

**報告を歓迎する例**: あるテナントの認可情報で別の名前空間のデータセットに到達できる経路、データセット名の推測や injection による越境。

### LLM に関する攻撃面

オントロジー候補の生成には LLM を使います。スキャンしたスキーマ・カラムのコメント・取り込んだ文書はいずれも**信頼できない入力**として扱う必要があります。

- 取り込んだ文書やスキーマのコメントに埋め込まれた指示による**プロンプトインジェクション**
- LLM が生成した OWL / SHACL / SPARQL をそのまま実行してしまう経路
- 設計上、LLM の出力は**必ず人間のレビューと承認を経てから**正本にコミットされます。この人間による承認を迂回できる経路があれば、それは脆弱性です

### 認証・認可

- 人間は Entra ID の認可コードフロー、エージェントは client credentials フロー、サービス間は Managed Identity を使います(PostgreSQL も Entra 認証でパスワードレス)
- **`AUTH_MODE=disabled`** は認証を完全に無効化する**ローカル開発専用**のオプションです。Azure にデプロイした環境でこのモードが有効化できてしまう経路、あるいは既定で有効になってしまう経路は脆弱性として扱います

### IaC とシークレット

`infra/` の Bicep が過剰な権限を付与している、シークレットがログや出力に露出する、Key Vault の参照が意図せず公開される、といった問題も脆弱性として報告してください。

---

## 対象外

以下は脆弱性として扱いません。

- **Phase 0 で機能が動作しないこと**(未実装であり仕様です)
- 利用者が意図的に `AUTH_MODE=disabled` を本番環境で設定した結果生じる問題
- 利用者が自身の SPARQL エンドポイント(`SPARQL_ENDPOINT`)として持ち込んだ第三者ストアの脆弱性(そのストアの開発元へ報告してください)
- 第三者コンポーネント(Apache Jena / Ontop など)自体の脆弱性。ただし**本プロジェクトの構成や既定値によって影響が増幅されている**場合は報告してください。上流の Advisory がある場合は、依存関係の更新として通常の Issue で構いません
- 実際の影響を伴わない、スキャナの出力のみに基づく報告
