# Phase 1 コア機能 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 名前空間を PostgreSQL に永続化し、Fuseki のデータセット単位で隔離したうえで、正本(Blob + PostgreSQL)からトリプルストアへ射影するループを動かす。

**Architecture:** 正本は PostgreSQL(メタデータ・監査)と Blob(バージョン付き TTL)。トリプルストアは再構築可能な射影であり、書き込み口は Core API のみ。名前空間ごとに Fuseki のデータセットを分けて物理的に隔離する。再起動時は Fuseki の entrypoint が Blob から各名前空間のデータセットを作り直し、API の reconcile が PostgreSQL 基準で差分を埋める。

**Tech Stack:** Python 3.12 / FastAPI / SQLAlchemy 2.0 (async, psycopg3) / Alembic / azure-storage-blob / azure-identity / Apache Jena Fuseki 6.2.0 (TDB2)

**Spec:**
- [`docs/architecture.md`](../../architecture.md)
- [`docs/adr/0001-rdf-store-selection.md`](../../adr/0001-rdf-store-selection.md) — 名前空間はデータセット単位で隔離する
- [`docs/adr/0002-triple-store-as-rebuildable-projection.md`](../../adr/0002-triple-store-as-rebuildable-projection.md) — 射影設計と entrypoint での再構築
- [`docs/adr/0003-postgresql-as-system-of-record.md`](../../adr/0003-postgresql-as-system-of-record.md) — 正本は PostgreSQL
- [`docs/adr/0006-ontology-versioning-and-audit.md`](../../adr/0006-ontology-versioning-and-audit.md) — 不変リビジョンと PROV-O 監査

## Global Constraints

- Python 3.12 以上。`uv` ワークスペース(`packages/*`)で管理する
- lint は `ruff`(line-length 100)、型検査は `mypy` strict。**両方通ることが各タスクの完了条件**
- 環境変数名の正本は `packages/core/src/ontology_core/config.py`。追加したら `infra/modules/*.bicep` も合わせる
- **トリプルストアへの書き込みは Core API のみ。** SPARQL Update / Graph Store Protocol を外部に公開しない
- **書き込み順序は「正本 → 射影」。** 逆にしない。射影の失敗は正本を巻き戻さず、再射影で回復する
- 名前空間名は `^[a-z0-9][a-z0-9-]{1,62}$` のみ許可する。Fuseki のデータセット名とファイルパスに使うため、境界で必ず検証する
- 新しい第三者依存を追加したら `docs/third-party-licenses.md` に追記する
- コミットメッセージは日本語。末尾に `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

---

## File Structure

**新規作成**

| ファイル | 責務 |
| --- | --- |
| `packages/core/src/ontology_core/db/__init__.py` | 公開シンボル |
| `packages/core/src/ontology_core/db/engine.py` | async エンジンとセッションの生成。Entra トークン認証のフォールバック |
| `packages/core/src/ontology_core/db/tables.py` | SQLAlchemy のテーブル定義(`namespaces` / `ontology_versions` / `audit_events`) |
| `packages/core/src/ontology_core/blob.py` | 正本 TTL の Blob 読み書き |
| `packages/core/src/ontology_core/graphs.py` | グラフ IRI とデータセット名の組み立て(唯一の正) |
| `packages/api/alembic.ini` | Alembic 設定 |
| `packages/api/alembic/env.py` | Alembic の async 実行設定 |
| `packages/api/alembic/versions/0001_initial.py` | 初期マイグレーション |
| `packages/api/src/ontology_api/repositories/__init__.py` | 公開シンボル |
| `packages/api/src/ontology_api/repositories/namespaces.py` | 名前空間の永続化 |
| `packages/api/src/ontology_api/repositories/versions.py` | オントロジーのバージョンと監査イベントの永続化 |
| `packages/api/src/ontology_api/services/__init__.py` | 公開シンボル |
| `packages/api/src/ontology_api/services/projection.py` | 射影ループ(正本 → Fuseki)と reconcile |
| `packages/api/src/ontology_api/routers/versions.py` | バージョンの投入・一覧 API |
| `packages/core/tests/test_graphs.py` | グラフ IRI 組み立ての単体テスト |
| `packages/api/tests/conftest.py` | DB フィクスチャ |
| `packages/api/tests/test_namespaces_repo.py` | 名前空間リポジトリ(integration) |
| `packages/api/tests/test_projection.py` | 射影ループ(integration) |
| `packages/api/tests/test_isolation.py` | 名前空間隔離の実証(integration) |

**変更**

| ファイル | 変更内容 |
| --- | --- |
| `packages/core/src/ontology_core/config.py` | Blob・DB 関連の設定を追加 |
| `packages/core/pyproject.toml` | `sqlalchemy` / `psycopg` / `azure-storage-blob` を追加 |
| `packages/api/pyproject.toml` | `alembic` を必須依存へ移動 |
| `packages/api/src/ontology_api/dependencies.py` | DB セッションと Blob クライアントの依存を追加 |
| `packages/api/src/ontology_api/routers/namespaces.py` | メモリスタブを削除し PostgreSQL + データセット作成に置換 |
| `packages/api/src/ontology_api/main.py` | `versions` ルータを登録 |
| `containers/fuseki/load-snapshot.sh` | 名前空間ごとに assembler と TDB2 を生成 |
| `justfile` | `test`(単体) / `test-integration` を分離、`migrate` を追加 |
| `.github/workflows/ci.yml` | PostgreSQL サービスを追加し integration テストを実行 |

---

## Task 1: グラフ IRI とデータセット名の組み立てを一箇所に集める

グラフ IRI の体系は複数の場所(API・ローダ・Bicep)から参照されるため、Python 側の唯一の正を作る。ここを最初に固めないと後続タスクで表記がぶれる。

**Files:**
- Create: `packages/core/src/ontology_core/graphs.py`
- Test: `packages/core/tests/test_graphs.py`

**Interfaces:**
- Consumes: `ontology_core.config.Settings.graph_iri_base`(Task 2 で追加。本タスクでは引数で受け取る形にして依存しない)
- Produces:
  - `dataset_name(namespace: str) -> str`
  - `version_graph_iri(base: str, namespace: str, version: str) -> str`
  - `NamespaceNameError`(不正な名前空間名)
  - `validate_namespace_name(namespace: str) -> str`

- [ ] **Step 1: 失敗するテストを書く**

```python
# packages/core/tests/test_graphs.py
"""グラフ IRI とデータセット名の組み立て。

名前空間名は Fuseki のデータセット名と Blob のパスに使うため、
境界で必ず検証する。ここが崩れると隔離が崩れる。
"""

from __future__ import annotations

import pytest

from ontology_core.graphs import (
    NamespaceNameError,
    dataset_name,
    validate_namespace_name,
    version_graph_iri,
)

BASE = "urn:ontology:graph"


def test_dataset_name_is_the_namespace_itself() -> None:
    assert dataset_name("retail-core") == "retail-core"


def test_version_graph_iri_includes_namespace_and_version() -> None:
    assert version_graph_iri(BASE, "retail-core", "1.0.0") == (
        "urn:ontology:graph/retail-core/1.0.0"
    )


def test_base_trailing_slash_is_normalised() -> None:
    assert version_graph_iri(BASE + "/", "retail-core", "1.0.0") == (
        "urn:ontology:graph/retail-core/1.0.0"
    )


@pytest.mark.parametrize(
    "bad",
    [
        "",
        "A",  # 大文字
        "-leading",  # 先頭がハイフン
        "has_underscore",
        "has space",
        "../escape",  # パストラバーサル
        "a/b",  # スラッシュ
        "x" * 64,  # 長すぎる
        "ds",  # Fuseki の予約データセット名(射影の作業用)
    ],
)
def test_invalid_namespace_names_are_rejected(bad: str) -> None:
    with pytest.raises(NamespaceNameError):
        validate_namespace_name(bad)


def test_valid_namespace_name_is_returned() -> None:
    assert validate_namespace_name("retail-core") == "retail-core"


def test_dataset_name_validates() -> None:
    with pytest.raises(NamespaceNameError):
        dataset_name("../escape")


def test_version_graph_iri_validates_namespace() -> None:
    with pytest.raises(NamespaceNameError):
        version_graph_iri(BASE, "../escape", "1.0.0")


@pytest.mark.parametrize("bad_version", ["", "../1.0.0", "1 0", "a/b"])
def test_invalid_versions_are_rejected(bad_version: str) -> None:
    with pytest.raises(NamespaceNameError):
        version_graph_iri(BASE, "retail-core", bad_version)
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `uv run pytest packages/core/tests/test_graphs.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'ontology_core.graphs'`

- [ ] **Step 3: 最小の実装を書く**

```python
# packages/core/src/ontology_core/graphs.py
"""グラフ IRI とデータセット名の組み立て。

名前空間名は次の 3 か所で使われる。
  - Fuseki のデータセット名(名前空間の物理的な隔離単位)
  - Blob のパス (`approved/<namespace>/<version>.ttl`)
  - 名前付きグラフ IRI

いずれもパストラバーサルや予期しない文字が入ると隔離が崩れるため、
**このモジュールを通してのみ**組み立てる。文字列連結を各所に散らさない。
"""

from __future__ import annotations

import re

__all__ = [
    "NamespaceNameError",
    "RESERVED_DATASET_NAMES",
    "dataset_name",
    "validate_namespace_name",
    "validate_version",
    "version_graph_iri",
]

# アンカー (`^...$`) を使わず fullmatch で判定する。
# Python の `re` は re.MULTILINE 未指定でも `$` が「文字列末尾」だけでなく
# 「末尾の改行の直前」にもマッチするため、`^...$` + match だと "ds\n" が通り、
# 後続の予約名チェック(完全一致)もすり抜けてしまう。fullmatch なら穴が
# 構造的に消える。ここを `^...$` に戻さないこと。
_NAMESPACE_PATTERN = re.compile(r"[a-z0-9][a-z0-9-]{1,62}")
_VERSION_PATTERN = re.compile(r"[A-Za-z0-9][A-Za-z0-9.+-]{0,63}")

# `ds` は射影の作業用に config.ttl が定義する固定データセット。
# 名前空間名として使わせない。
RESERVED_DATASET_NAMES = frozenset({"ds"})


class NamespaceNameError(ValueError):
    """名前空間名またはバージョン文字列が使えないことを表す。"""


def validate_namespace_name(namespace: str) -> str:
    """名前空間名を検証して返す。

    Raises:
        NamespaceNameError: 形式が不正、または予約名のとき。
    """
    if not _NAMESPACE_PATTERN.fullmatch(namespace):
        raise NamespaceNameError(
            f"名前空間名 '{namespace}' は使えません。"
            "小文字英数字とハイフンのみ、2〜63 文字、先頭は英数字です"
        )
    if namespace in RESERVED_DATASET_NAMES:
        raise NamespaceNameError(f"名前空間名 '{namespace}' は予約されています")
    return namespace


def validate_version(version: str) -> str:
    """バージョン文字列を検証して返す。

    Raises:
        NamespaceNameError: IRI やパスに使えない文字を含むとき。
    """
    if not _VERSION_PATTERN.fullmatch(version):
        raise NamespaceNameError(f"バージョン '{version}' は使えません")
    return version


def dataset_name(namespace: str) -> str:
    """名前空間に対応する Fuseki のデータセット名を返す。"""
    return validate_namespace_name(namespace)


def version_graph_iri(base: str, namespace: str, version: str) -> str:
    """バージョンごとの名前付きグラフ IRI を返す。"""
    validate_namespace_name(namespace)
    validate_version(version)
    return f"{base.rstrip('/')}/{namespace}/{version}"
```

- [ ] **Step 4: テストが通ることを確認**

Run: `uv run pytest packages/core/tests/test_graphs.py -v`
Expected: PASS(13 件)

- [ ] **Step 5: lint と型検査**

Run: `uv run ruff check . && uv run ruff format --check . && uv run mypy packages`
Expected: すべて成功

- [ ] **Step 6: コミット**

```bash
git add packages/core/src/ontology_core/graphs.py packages/core/tests/test_graphs.py
git commit -m "$(cat <<'EOF'
feat(core): グラフIRIとデータセット名の組み立てを一箇所に集める

名前空間名はFusekiのデータセット名・Blobのパス・グラフIRIの3か所で
使われ、パストラバーサルが入ると隔離が崩れる。文字列連結を各所に
散らさず、このモジュールを通してのみ組み立てる。

`ds` は射影の作業用データセットなので予約名として弾く。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 設定と依存関係を追加する

DB と Blob を扱うための設定と依存を先に入れる。以降のタスクがこれに依存する。

**Files:**
- Modify: `packages/core/pyproject.toml`
- Modify: `packages/api/pyproject.toml`
- Modify: `packages/core/src/ontology_core/config.py`
- Modify: `docs/third-party-licenses.md`

**Interfaces:**
- Produces:
  - `Settings.graph_iri_base: str`(`GRAPH_IRI_BASE`、既定 `urn:ontology:graph`)
  - `Settings.database_url: str`(`DATABASE_URL`。空なら `postgres_*` から組み立てる)
  - `Settings.async_postgres_dsn: str`(property。`postgresql+psycopg://...`)

- [ ] **Step 1: core の依存を追加**

`packages/core/pyproject.toml` の `dependencies` に追記する。

```toml
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.2",
    "azure-storage-blob>=12.24",
```

- [ ] **Step 2: api の依存から postgres extra を外し alembic を必須にする**

`packages/api/pyproject.toml` の `[project.optional-dependencies]` ブロックを削除し、`dependencies` に `"alembic>=1.14",` を追記する。SQLAlchemy と psycopg は core 経由で入る。

- [ ] **Step 3: 設定を追加**

`packages/core/src/ontology_core/config.py` の「正本(PostgreSQL)」ブロックの直後に追記する。

```python
    # 接続文字列を直接与えたい場合に使う。空なら postgres_* から組み立てる。
    database_url: str = Field(default="", alias="DATABASE_URL")
```

「正本(Blob)」ブロックに追記する。

```python
    # 名前付きグラフ IRI の接頭辞。containers/fuseki/load-snapshot.sh と
    # infra/modules/fuseki.bicep の graphIriBase と同じ値でなければならない。
    graph_iri_base: str = Field(default="urn:ontology:graph", alias="GRAPH_IRI_BASE")
    # 承認済み TTL を置く Blob のプレフィックス。ローダの BLOB_PREFIX と揃える。
    ontology_blob_prefix: str = Field(default="approved/", alias="BLOB_PREFIX")
```

`postgres_dsn` property の直後に追記する。

```python
    @property
    def async_postgres_dsn(self) -> str:
        """SQLAlchemy async エンジン用の DSN。

        `DATABASE_URL` が与えられていればそれを優先する。パスワードが空の場合は
        Entra ID のトークンを実行時に取得して渡すため、DSN にはパスワードを含めない。
        """
        if self.database_url:
            return self.database_url
        return (
            f"postgresql+psycopg://{self.postgres_user}@"
            f"{self.postgres_host}:{self.postgres_port}/{self.postgres_database}"
        )
```

- [ ] **Step 4: 依存を解決してテストが壊れていないことを確認**

Run: `uv sync --all-packages && uv run pytest && uv run mypy packages`
Expected: 既存の 23 件が PASS、mypy 成功

- [ ] **Step 5: ライセンス表に追記**

`docs/third-party-licenses.md` の「採用するコンポーネント」表に 3 行追加する。

```markdown
| SQLAlchemy | MIT | https://github.com/sqlalchemy/sqlalchemy | ✓ 正本(PostgreSQL)へのアクセス |
| psycopg | LGPL-3.0 (ライブラリ本体) / psycopg-binary は同梱 libpq に依存 | https://github.com/psycopg/psycopg | △ **要確認**: psycopg3 本体は LGPL-3.0。別プロセス化できないため、代替(asyncpg = Apache-2.0)への切り替えを検討する。Task 2 の完了条件に含める |
| azure-storage-blob | MIT | https://github.com/Azure/azure-sdk-for-python | ✓ 正本 TTL の読み書き |
```

- [ ] **Step 6: psycopg のライセンスを確認して結論を出す**

`psycopg` (psycopg3) は **LGPL-3.0** である(GitHub API で確認済み: psycopg/psycopg = LGPL-3.0、MagicStack/asyncpg = Apache-2.0、sqlalchemy = MIT)。Apache-2.0 の本体にライブラリとして取り込むため、[ADR-0005](../../adr/0005-reasoner-boundary.md) で owlready2 を却下したのと同じ論点が生じる。

判断すること: `asyncpg`(Apache-2.0)へ切り替えるか、psycopg のままにするか。
- 切り替える場合: DSN を `postgresql+asyncpg://` に変え、`packages/core/pyproject.toml` の依存を差し替える。Entra トークンをパスワードとして渡す方法は両者で同じ
- **既定の判断: `asyncpg` へ切り替える。** ライセンスの一貫性(Apache-2.0 互換のみ)を保つ方針を [ADR-0005] で明示済みであり、ここで例外を作らない

切り替えを実施し、`docs/third-party-licenses.md` の psycopg 行を asyncpg 行に置き換える。

```markdown
| asyncpg | Apache-2.0 | https://github.com/MagicStack/asyncpg | ✓ 正本(PostgreSQL)への async アクセス。psycopg3 は LGPL-3.0 のため採用しない |
```

`Settings.async_postgres_dsn` と `postgres_dsn` のスキームを `postgresql+asyncpg://` に変更する。

- [ ] **Step 7: コミット**

```bash
git add packages/core/pyproject.toml packages/api/pyproject.toml \
        packages/core/src/ontology_core/config.py docs/third-party-licenses.md uv.lock
git commit -m "$(cat <<'EOF'
feat(core): DBとBlobの設定・依存を追加し、DBドライバをasyncpgにする

Phase 1 コアで必要になる設定 (DATABASE_URL / GRAPH_IRI_BASE /
BLOB_PREFIX) と依存 (sqlalchemy, asyncpg, azure-storage-blob) を追加。

DBドライバは psycopg3 ではなく asyncpg を選ぶ。psycopg3 は LGPL-3.0 で、
別プロセス化できないライブラリとしてApache-2.0の本体に取り込むと
owlready2 を却下したのと同じ論点が生じるため。asyncpg は Apache-2.0。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: テーブル定義と Alembic マイグレーション

**Files:**
- Create: `packages/core/src/ontology_core/db/__init__.py`
- Create: `packages/core/src/ontology_core/db/tables.py`
- Create: `packages/core/src/ontology_core/db/engine.py`
- Create: `packages/api/alembic.ini`
- Create: `packages/api/alembic/env.py`
- Create: `packages/api/alembic/script.py.mako`
- Create: `packages/api/alembic/versions/0001_initial.py`
- Modify: `justfile`

**Interfaces:**
- Consumes: `ontology_core.config.get_settings`
- Produces:
  - `ontology_core.db.Base`(DeclarativeBase)
  - `ontology_core.db.NamespaceRow` / `OntologyVersionRow` / `AuditEventRow`
  - `ontology_core.db.create_session_factory(dsn: str) -> async_sessionmaker[AsyncSession]`
  - `ontology_core.db.dispose_engine() -> None`

- [ ] **Step 1: テーブル定義を書く**

```python
# packages/core/src/ontology_core/db/tables.py
"""正本(PostgreSQL)のテーブル定義。

ここに置くのは「オントロジーそのもの」ではなく、その**メタデータと監査**である。
オントロジー本体(TTL)の正本は Blob 側にあり、DB は所在と状態を持つ
(docs/adr/0003-postgresql-as-system-of-record.md)。
"""

from __future__ import annotations

from datetime import datetime

from sqlalchemy import DateTime, ForeignKey, Index, String, Text, UniqueConstraint, func
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column


class Base(DeclarativeBase):
    """全テーブルの基底。"""


class NamespaceRow(Base):
    """名前空間。Fuseki のデータセット 1 つに対応する。"""

    __tablename__ = "namespaces"

    name: Mapped[str] = mapped_column(String(63), primary_key=True)
    display_name: Mapped[str] = mapped_column(String(255))
    description: Mapped[str] = mapped_column(Text, default="")
    base_iri: Mapped[str] = mapped_column(Text)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    created_by: Mapped[str] = mapped_column(String(255))


class OntologyVersionRow(Base):
    """オントロジーの不変リビジョン。

    同一の名前空間で同じ content_hash を二重登録しない(冪等性の根拠)。
    """

    __tablename__ = "ontology_versions"
    __table_args__ = (
        UniqueConstraint("namespace", "version", name="uq_ontology_versions_ns_version"),
        UniqueConstraint("namespace", "content_hash", name="uq_ontology_versions_ns_hash"),
        Index("ix_ontology_versions_namespace", "namespace"),
    )

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    namespace: Mapped[str] = mapped_column(
        ForeignKey("namespaces.name", ondelete="CASCADE"), nullable=False
    )
    version: Mapped[str] = mapped_column(String(64))
    content_hash: Mapped[str] = mapped_column(String(64))
    status: Mapped[str] = mapped_column(String(32))
    graph_iri: Mapped[str] = mapped_column(Text)
    blob_path: Mapped[str] = mapped_column(Text)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    created_by: Mapped[str] = mapped_column(String(255))
    approved_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), default=None)
    approved_by: Mapped[str | None] = mapped_column(String(255), default=None)
    # 射影が完了した時刻。NULL なら未射影で、reconcile の対象になる。
    projected_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), default=None)


class AuditEventRow(Base):
    """監査証跡。誰が・いつ・何を・なぜ。"""

    __tablename__ = "audit_events"
    __table_args__ = (Index("ix_audit_events_namespace_occurred", "namespace", "occurred_at"),)

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    namespace: Mapped[str] = mapped_column(String(63), nullable=False)
    action: Mapped[str] = mapped_column(String(64))
    actor: Mapped[str] = mapped_column(String(255))
    occurred_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )
    subject: Mapped[str] = mapped_column(Text)
    reason: Mapped[str] = mapped_column(Text, default="")
    diff: Mapped[str | None] = mapped_column(Text, default=None)
```

- [ ] **Step 2: エンジン生成を書く**

```python
# packages/core/src/ontology_core/db/engine.py
"""async エンジンとセッションの生成。

パスワードが設定されていない場合は Entra ID のアクセストークンをパスワードとして
渡す(PostgreSQL Flexible Server のパスワードレス接続)。トークンは有効期限が
あるため、接続のたびに取得する。
"""

from __future__ import annotations

from collections.abc import AsyncIterator
from typing import Any

from sqlalchemy.ext.asyncio import (
    AsyncEngine,
    AsyncSession,
    async_sessionmaker,
    create_async_engine,
)

from ontology_core.config import Settings

__all__ = ["create_engine_and_factory", "session_scope"]

# Azure Database for PostgreSQL のトークン対象スコープ。
_POSTGRES_SCOPE = "https://ossrdbms-aad.database.windows.net/.default"


def _entra_password_provider() -> str:
    """Entra ID のアクセストークンを取得してパスワードとして返す。"""
    from azure.identity import DefaultAzureCredential

    credential = DefaultAzureCredential()
    return credential.get_token(_POSTGRES_SCOPE).token


def create_engine_and_factory(
    settings: Settings,
) -> tuple[AsyncEngine, async_sessionmaker[AsyncSession]]:
    """エンジンとセッションファクトリを作る。

    パスワードが空のときは接続ごとに Entra トークンを取得する。
    """
    connect_args: dict[str, Any] = {}
    if settings.postgres_password:
        connect_args["password"] = settings.postgres_password
    elif not settings.database_url:
        # DSN にパスワードを埋めず、接続時にトークンを渡す。
        connect_args["password"] = _entra_password_provider

    engine = create_async_engine(
        settings.async_postgres_dsn,
        pool_pre_ping=True,
        pool_size=5,
        max_overflow=5,
        connect_args=connect_args,
    )
    factory = async_sessionmaker(engine, expire_on_commit=False)
    return engine, factory


async def session_scope(
    factory: async_sessionmaker[AsyncSession],
) -> AsyncIterator[AsyncSession]:
    """1 リクエスト 1 トランザクション。例外時はロールバックする。"""
    async with factory() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
```

**確認済み:** `asyncpg` は `connect_args["password"]` に **callable を受け付け、接続のたびに
呼び出す**。SQLAlchemy 2.0 + asyncpg 0.31 + PostgreSQL 16 で実測した(callable が呼ばれた
ことと `SELECT 1` が通ることを確認)。したがって上記の実装をそのまま使ってよい。

トークンには有効期限があるため、接続のたびに取得されるこの挙動が要件に合っている。
接続文字列にパスワードを埋め込む形にしてはならない(期限切れで接続不能になる)。

- [ ] **Step 3: `__init__.py` を書く**

```python
# packages/core/src/ontology_core/db/__init__.py
"""正本(PostgreSQL)へのアクセス層。"""

from ontology_core.db.engine import create_engine_and_factory, session_scope
from ontology_core.db.tables import AuditEventRow, Base, NamespaceRow, OntologyVersionRow

__all__ = [
    "AuditEventRow",
    "Base",
    "NamespaceRow",
    "OntologyVersionRow",
    "create_engine_and_factory",
    "session_scope",
]
```

- [ ] **Step 4: Alembic を初期化する**

Run: `cd packages/api && uv run alembic init -t async alembic`

生成された `alembic/env.py` の `target_metadata` を差し替える。

```python
from ontology_core.config import get_settings
from ontology_core.db import Base

target_metadata = Base.metadata


def get_url() -> str:
    return get_settings().async_postgres_dsn
```

`run_async_migrations` 内の `config.get_main_option("sqlalchemy.url")` を `get_url()` に置き換え、
`alembic.ini` の `sqlalchemy.url` は空にする(設定は環境変数から取る)。

- [ ] **Step 5: 初期マイグレーションを生成して中身を確認する**

Run:
```bash
docker compose up -d postgres
cd packages/api
POSTGRES_PASSWORD=localdev uv run alembic revision --autogenerate -m "initial"
```

生成されたファイルを `alembic/versions/0001_initial.py` にリネームし、
`namespaces` / `ontology_versions` / `audit_events` の 3 テーブルと
2 つの UniqueConstraint、3 つの Index が含まれていることを目で確認する。

- [ ] **Step 6: マイグレーションを適用して確認**

Run:
```bash
cd packages/api && POSTGRES_PASSWORD=localdev uv run alembic upgrade head
docker compose exec -T postgres psql -U ontology -d ontology -c '\dt'
```
Expected: `alembic_version` / `audit_events` / `namespaces` / `ontology_versions` が並ぶ

- [ ] **Step 7: justfile にタスクを追加**

```makefile
# DBマイグレーションを適用する(要: just up)
migrate:
    cd packages/api && uv run alembic upgrade head

# 新しいマイグレーションを生成する
migrate-new message:
    cd packages/api && uv run alembic revision --autogenerate -m "{{message}}"
```

- [ ] **Step 8: lint と型検査**

Run: `uv run ruff check . && uv run ruff format --check . && uv run mypy packages`
Expected: すべて成功(Alembic の生成ファイルが引っかかる場合は `pyproject.toml` の
`[tool.ruff.lint.per-file-ignores]` に `"packages/api/alembic/**" = ["ALL"]` を追加し、
`[tool.mypy]` の `exclude` に `packages/api/alembic/versions/` を追加する)

- [ ] **Step 9: コミット**

```bash
git add packages/core/src/ontology_core/db packages/api/alembic packages/api/alembic.ini \
        justfile pyproject.toml
git commit -m "$(cat <<'EOF'
feat(core): 正本のテーブル定義とAlembicマイグレーションを追加

namespaces / ontology_versions / audit_events の3テーブル。
DBが持つのはオントロジー本体ではなくメタデータと監査で、TTLの正本は
Blob側にある (ADR-0003)。

ontology_versions は (namespace, content_hash) に一意制約を張り、
同一内容の二重登録を弾く。射影の完了時刻 projected_at を持たせ、
NULLのものをreconcileの対象にする。

パスワードが空の場合はEntra IDのトークンをパスワードとして渡す
パスワードレス接続に対応。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 名前空間を PostgreSQL に永続化し、データセットを作る

メモリスタブを置き換える。名前空間の作成は「DB に書く → Fuseki にデータセットを作る」の順で行う。

**Files:**
- Create: `packages/api/src/ontology_api/repositories/__init__.py`
- Create: `packages/api/src/ontology_api/repositories/namespaces.py`
- Create: `packages/api/tests/conftest.py`
- Create: `packages/api/tests/test_namespaces_repo.py`
- Modify: `packages/api/src/ontology_api/dependencies.py`
- Modify: `packages/api/src/ontology_api/routers/namespaces.py`
- Modify: `pyproject.toml`(pytest マーカー)
- Modify: `justfile`
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: `ontology_core.db`(Task 3)、`ontology_core.graphs.dataset_name`(Task 1)
- Produces:
  - `NamespaceRepository(session: AsyncSession)`
    - `async def create(self, *, name, display_name, description, base_iri, created_by) -> Namespace`
    - `async def get(self, name: str) -> Namespace | None`
    - `async def list_all(self) -> list[Namespace]`
    - `async def delete(self, name: str) -> bool`
  - `NamespaceExistsError`
  - `SessionDep`(FastAPI 依存)

- [ ] **Step 1: pytest マーカーと DB フィクスチャを用意する**

`pyproject.toml` の `[tool.pytest.ini_options]` に追記する。

```toml
markers = [
    "integration: 実際の PostgreSQL を必要とするテスト",
]
```

```python
# packages/api/tests/conftest.py
"""テスト用のフィクスチャ。

integration マークのテストは実際の PostgreSQL を使う。ローカルでは
`just up` で立てた compose の PostgreSQL を、CI では services の
PostgreSQL を使う。**スキップはしない** — 接続できなければ失敗させる。
静かにスキップされたテストは、通っているように見えて何も検証しない。
"""

from __future__ import annotations

import os
from collections.abc import AsyncIterator

import pytest
import pytest_asyncio
from ontology_core.config import Settings
from ontology_core.db import Base, create_engine_and_factory
from sqlalchemy.ext.asyncio import AsyncSession


def _test_settings() -> Settings:
    return Settings(
        POSTGRES_HOST=os.environ.get("POSTGRES_HOST", "localhost"),
        POSTGRES_PORT=int(os.environ.get("POSTGRES_PORT", "5432")),
        POSTGRES_DATABASE=os.environ.get("POSTGRES_DATABASE", "ontology"),
        POSTGRES_USER=os.environ.get("POSTGRES_USER", "ontology"),
        POSTGRES_PASSWORD=os.environ.get("POSTGRES_PASSWORD", "localdev"),
        AUTH_MODE="disabled",
    )


@pytest_asyncio.fixture
async def session() -> AsyncIterator[AsyncSession]:
    """テーブルを作り直したまっさらな DB のセッションを返す。"""
    engine, factory = create_engine_and_factory(_test_settings())
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.drop_all)
        await conn.run_sync(Base.metadata.create_all)
    async with factory() as s:
        yield s
    await engine.dispose()


@pytest.fixture
def settings() -> Settings:
    return _test_settings()
```

- [ ] **Step 2: 失敗するテストを書く**

```python
# packages/api/tests/test_namespaces_repo.py
"""名前空間リポジトリ。"""

from __future__ import annotations

import pytest
from ontology_core.graphs import NamespaceNameError
from sqlalchemy.ext.asyncio import AsyncSession

from ontology_api.repositories.namespaces import NamespaceExistsError, NamespaceRepository

pytestmark = pytest.mark.integration


async def test_create_then_get(session: AsyncSession) -> None:
    repo = NamespaceRepository(session)

    created = await repo.create(
        name="retail-core",
        display_name="小売ドメイン",
        description="デモ",
        base_iri="https://example.com/ontology/retail#",
        created_by="tester",
    )
    assert created.name == "retail-core"
    assert created.display_name == "小売ドメイン"

    fetched = await repo.get("retail-core")
    assert fetched is not None
    assert fetched.base_iri == "https://example.com/ontology/retail#"


async def test_create_is_rejected_when_duplicated(session: AsyncSession) -> None:
    repo = NamespaceRepository(session)
    kwargs = dict(display_name="x", description="", base_iri="https://e.example/#", created_by="t")
    await repo.create(name="dup", **kwargs)  # type: ignore[arg-type]

    with pytest.raises(NamespaceExistsError):
        await repo.create(name="dup", **kwargs)  # type: ignore[arg-type]


async def test_invalid_name_is_rejected_before_touching_db(session: AsyncSession) -> None:
    repo = NamespaceRepository(session)
    with pytest.raises(NamespaceNameError):
        await repo.create(
            name="../escape",
            display_name="x",
            description="",
            base_iri="https://e.example/#",
            created_by="t",
        )


async def test_list_and_delete(session: AsyncSession) -> None:
    repo = NamespaceRepository(session)
    for name in ("alpha", "beta"):
        await repo.create(
            name=name,
            display_name=name,
            description="",
            base_iri=f"https://e.example/{name}#",
            created_by="t",
        )

    assert [n.name for n in await repo.list_all()] == ["alpha", "beta"]
    assert await repo.delete("alpha") is True
    assert [n.name for n in await repo.list_all()] == ["beta"]
    assert await repo.delete("alpha") is False


async def test_persists_across_sessions(session: AsyncSession) -> None:
    # メモリスタブではないこと(= 別のセッションから見えること)を確認する。
    repo = NamespaceRepository(session)
    await repo.create(
        name="persisted",
        display_name="p",
        description="",
        base_iri="https://e.example/p#",
        created_by="t",
    )
    await session.commit()

    other = NamespaceRepository(session)
    session.expunge_all()
    assert await other.get("persisted") is not None
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `docker compose up -d postgres && uv run pytest packages/api/tests/test_namespaces_repo.py -v`
Expected: FAIL — `ModuleNotFoundError: ontology_api.repositories`

- [ ] **Step 4: リポジトリを実装する**

```python
# packages/api/src/ontology_api/repositories/namespaces.py
"""名前空間の永続化。

名前空間名は Fuseki のデータセット名になるため、**DB に触る前に**検証する。
"""

from __future__ import annotations

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from ontology_core.db import NamespaceRow
from ontology_core.graphs import validate_namespace_name
from ontology_core.models import Namespace

__all__ = ["NamespaceExistsError", "NamespaceRepository"]


class NamespaceExistsError(Exception):
    """同名の名前空間が既に存在することを表す。"""


def _to_model(row: NamespaceRow) -> Namespace:
    return Namespace(
        name=row.name,
        display_name=row.display_name,
        description=row.description,
        base_iri=row.base_iri,
        created_at=row.created_at,
        created_by=row.created_by,
    )


class NamespaceRepository:
    """名前空間テーブルへのアクセス。"""

    def __init__(self, session: AsyncSession) -> None:
        self._session = session

    async def create(
        self,
        *,
        name: str,
        display_name: str,
        description: str,
        base_iri: str,
        created_by: str,
    ) -> Namespace:
        """名前空間を作る。

        Raises:
            NamespaceNameError: 名前が不正なとき。
            NamespaceExistsError: 既に存在するとき。
        """
        validate_namespace_name(name)
        if await self.get(name) is not None:
            raise NamespaceExistsError(f"名前空間 '{name}' は既に存在します")

        row = NamespaceRow(
            name=name,
            display_name=display_name,
            description=description,
            base_iri=base_iri,
            created_by=created_by,
        )
        self._session.add(row)
        await self._session.flush()
        await self._session.refresh(row)
        return _to_model(row)

    async def get(self, name: str) -> Namespace | None:
        row = await self._session.get(NamespaceRow, name)
        return _to_model(row) if row is not None else None

    async def list_all(self) -> list[Namespace]:
        result = await self._session.execute(select(NamespaceRow).order_by(NamespaceRow.name))
        return [_to_model(row) for row in result.scalars()]

    async def delete(self, name: str) -> bool:
        """削除する。存在しなければ False を返す。"""
        row = await self._session.get(NamespaceRow, name)
        if row is None:
            return False
        await self._session.delete(row)
        await self._session.flush()
        return True
```

```python
# packages/api/src/ontology_api/repositories/__init__.py
"""正本へのアクセスをまとめる層。"""

from ontology_api.repositories.namespaces import NamespaceExistsError, NamespaceRepository

__all__ = ["NamespaceExistsError", "NamespaceRepository"]
```

- [ ] **Step 5: テストが通ることを確認**

Run: `uv run pytest packages/api/tests/test_namespaces_repo.py -v`
Expected: PASS(5 件)

- [ ] **Step 6: DB セッションの依存を追加する**

`packages/api/src/ontology_api/dependencies.py` に追記する。

```python
from functools import lru_cache

from ontology_core.db import create_engine_and_factory, session_scope
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker


@lru_cache(maxsize=1)
def _session_factory() -> async_sessionmaker[AsyncSession]:
    """プロセス内で 1 つのエンジンを共有する。"""
    _engine, factory = create_engine_and_factory(get_settings())
    return factory


async def db_session() -> AsyncIterator[AsyncSession]:
    async for session in session_scope(_session_factory()):
        yield session


SessionDep = Annotated[AsyncSession, Depends(db_session)]
```

`__all__` に `"SessionDep"` を追加する。

- [ ] **Step 7: ルータをリポジトリ + データセット作成に差し替える**

`packages/api/src/ontology_api/routers/namespaces.py` の `_IN_MEMORY_STUB` を削除し、
各エンドポイントを書き換える。**作成は「DB に書く → データセットを作る」の順**にする。

```python
@router.post("", status_code=status.HTTP_201_CREATED, summary="名前空間を作成する")
async def create_namespace(
    payload: NamespaceCreate,
    principal: CurrentPrincipal,
    session: SessionDep,
    store: StoreDep,
) -> Namespace:
    """名前空間を作成し、対応する Fuseki データセットを用意する。

    順序は「正本(DB)に書く → 射影先(データセット)を作る」。逆にしない。
    データセット作成に失敗した場合も名前空間は残る。reconcile が後から埋める。
    """
    repo = NamespaceRepository(session)
    try:
        namespace = await repo.create(
            name=payload.name,
            display_name=payload.display_name,
            description=payload.description,
            base_iri=payload.base_iri,
            created_by=principal.object_id or principal.subject,
        )
    except NamespaceNameError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc
    except NamespaceExistsError as exc:
        raise HTTPException(status_code=status.HTTP_409_CONFLICT, detail=str(exc)) from exc

    try:
        await store.create_dataset(dataset_name(namespace.name))
    except SparqlStoreError:
        logger.exception(
            "名前空間 '%s' のデータセット作成に失敗しました。reconcile で回復します",
            namespace.name,
        )

    return namespace
```

削除も同様に「DB から消す → データセットを消す」の順にする。一覧・取得はリポジトリに委譲する。

- [ ] **Step 8: 既存のスタブ往復テストを差し替える**

`packages/api/tests/test_health.py` の `test_namespace_roundtrip_on_stub` を削除する
(DB を使う往復テストは `test_namespaces_repo.py` が担う)。`test_healthz_returns_ok`、
`test_openapi_schema_is_generated`、`test_update_query_is_rejected` は残す。

- [ ] **Step 9: justfile と CI を分離する**

justfile:

```makefile
# 単体テスト(DB不要)
test:
    uv run pytest -m "not integration"

# integrationテスト(要: just up)
test-integration:
    uv run pytest -m integration
```

`check` を `lint typecheck test` のままにし、`check-all: lint typecheck test test-integration` を追加する。

`.github/workflows/ci.yml` の `python` ジョブに PostgreSQL サービスを追加し、両方を実行する。

```yaml
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: ontology
          POSTGRES_USER: ontology
          POSTGRES_PASSWORD: localdev
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready --health-interval 10s
          --health-timeout 5s --health-retries 5
```

テストステップを差し替える。

```yaml
      - name: テスト(単体 + integration)
        env:
          POSTGRES_PASSWORD: localdev
        run: uv run pytest
```

- [ ] **Step 10: 全テストと lint**

Run: `uv run pytest && uv run ruff check . && uv run mypy packages`
Expected: すべて成功

- [ ] **Step 11: コミット**

```bash
git add packages/api pyproject.toml justfile .github/workflows/ci.yml
git commit -m "$(cat <<'EOF'
feat(api): 名前空間をPostgreSQLに永続化しデータセットを作る

メモリ上のスタブを実装に置き換えた。作成順序は「正本(DB)に書く →
射影先(Fusekiデータセット)を作る」で固定し、逆にしない。データセット
作成に失敗しても名前空間は残り、reconcileが後から埋める。

名前空間名はFusekiのデータセット名になるため、DBに触る前に検証する。

integrationテストは実PostgreSQLを使い、スキップしない。静かにスキップ
されたテストは通っているように見えて何も検証しないため。CIにpostgres
サービスを追加した。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 正本 Blob クライアント

**Files:**
- Create: `packages/core/src/ontology_core/blob.py`
- Test: `packages/api/tests/test_blob.py`

**Interfaces:**
- Produces:
  - `OntologyBlobStore(account_url: str, container: str, prefix: str, credential=None)`
    - `async def put_version(self, namespace: str, version: str, turtle: str) -> str`(戻り値は blob path)
    - `async def get_version(self, blob_path: str) -> str`
    - `async def list_versions(self, namespace: str | None = None) -> list[str]`
    - `async def aclose(self) -> None`
  - `blob_path_for(prefix: str, namespace: str, version: str) -> str`

- [ ] **Step 1: 失敗するテストを書く**

Azurite(Blob エミュレータ)を compose に足して実物で検証する。モックにしない
(SDK の使い方の誤りはモックでは出ない)。

`docker-compose.yml` に追記する。

```yaml
  azurite:
    image: mcr.microsoft.com/azure-storage/azurite:latest
    command: ["azurite-blob", "--blobHost", "0.0.0.0", "--skipApiVersionCheck"]
    ports:
      - "${AZURITE_PORT:-10000}:10000"
```

```python
# packages/api/tests/test_blob.py
"""正本 Blob クライアント。Azurite に対して実際に読み書きする。"""

from __future__ import annotations

import pytest
from azure.storage.blob.aio import BlobServiceClient
from ontology_core.blob import OntologyBlobStore, blob_path_for

pytestmark = pytest.mark.integration

# Azurite の既定アカウント。
_CONN = (
    "DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;"
    "AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;"
    "BlobEndpoint=http://localhost:10000/devstoreaccount1;"
)


@pytest.fixture
async def store() -> OntologyBlobStore:
    service = BlobServiceClient.from_connection_string(_CONN)
    container = "ontologies-test"
    try:
        await service.create_container(container)
    except Exception:  # 既存なら無視
        pass
    s = OntologyBlobStore.from_client(service, container=container, prefix="approved/")
    yield s
    await s.aclose()


def test_blob_path_layout() -> None:
    assert blob_path_for("approved/", "retail-core", "1.0.0") == "approved/retail-core/1.0.0.ttl"


def test_blob_path_rejects_traversal() -> None:
    from ontology_core.graphs import NamespaceNameError

    with pytest.raises(NamespaceNameError):
        blob_path_for("approved/", "../escape", "1.0.0")


async def test_put_then_get_roundtrip(store: OntologyBlobStore) -> None:
    ttl = "@prefix ex: <https://e.example/#> .\nex:A a ex:B .\n"
    path = await store.put_version("retail-core", "1.0.0", ttl)

    assert path == "approved/retail-core/1.0.0.ttl"
    assert await store.get_version(path) == ttl


async def test_list_versions_filters_by_namespace(store: OntologyBlobStore) -> None:
    await store.put_version("alpha", "1.0.0", "# a\n")
    await store.put_version("beta", "1.0.0", "# b\n")

    assert await store.list_versions("alpha") == ["approved/alpha/1.0.0.ttl"]
    listed = await store.list_versions()
    assert "approved/beta/1.0.0.ttl" in listed
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `docker compose up -d azurite && uv run pytest packages/api/tests/test_blob.py -v`
Expected: FAIL — `ModuleNotFoundError: ontology_core.blob`

- [ ] **Step 3: 実装する**

```python
# packages/core/src/ontology_core/blob.py
"""正本 TTL の Blob 読み書き。

Blob のレイアウトは `<prefix><namespace>/<version>.ttl`。
Fuseki のローダ(containers/fuseki/load-snapshot.sh)がこのレイアウトを前提に
名前空間ごとのデータセットを組み立てるため、**変更するときは両方を直す**。
"""

from __future__ import annotations

from typing import Self

from azure.storage.blob.aio import BlobServiceClient, ContainerClient

from ontology_core.graphs import validate_namespace_name, validate_version

__all__ = ["OntologyBlobStore", "blob_path_for"]


def blob_path_for(prefix: str, namespace: str, version: str) -> str:
    """正本 TTL の Blob 上のパスを返す。"""
    validate_namespace_name(namespace)
    validate_version(version)
    return f"{prefix.rstrip('/')}/{namespace}/{version}.ttl"


class OntologyBlobStore:
    """承認済みオントロジー(正本)の置き場。"""

    def __init__(self, container_client: ContainerClient, *, prefix: str) -> None:
        self._container = container_client
        self._prefix = prefix

    @classmethod
    def from_account_url(
        cls, account_url: str, *, container: str, prefix: str, credential: object
    ) -> Self:
        service = BlobServiceClient(account_url=account_url, credential=credential)  # type: ignore[arg-type]
        return cls(service.get_container_client(container), prefix=prefix)

    @classmethod
    def from_client(cls, service: BlobServiceClient, *, container: str, prefix: str) -> Self:
        return cls(service.get_container_client(container), prefix=prefix)

    async def put_version(self, namespace: str, version: str, turtle: str) -> str:
        """TTL を置いて Blob パスを返す。"""
        path = blob_path_for(self._prefix, namespace, version)
        blob = self._container.get_blob_client(path)
        await blob.upload_blob(turtle.encode("utf-8"), overwrite=True)
        return path

    async def get_version(self, blob_path: str) -> str:
        blob = self._container.get_blob_client(blob_path)
        stream = await blob.download_blob()
        return (await stream.readall()).decode("utf-8")

    async def list_versions(self, namespace: str | None = None) -> list[str]:
        """TTL の Blob パスを列挙する。"""
        if namespace is not None:
            validate_namespace_name(namespace)
            name_prefix = f"{self._prefix.rstrip('/')}/{namespace}/"
        else:
            name_prefix = self._prefix
        names: list[str] = []
        async for blob in self._container.list_blobs(name_starts_with=name_prefix):
            if blob.name.endswith(".ttl"):
                names.append(blob.name)
        return sorted(names)

    async def aclose(self) -> None:
        await self._container.close()
```

- [ ] **Step 4: テストが通ることを確認**

Run: `uv run pytest packages/api/tests/test_blob.py -v`
Expected: PASS(4 件)

- [ ] **Step 5: lint と型検査、コミット**

```bash
uv run ruff check . && uv run ruff format --check . && uv run mypy packages
git add packages/core/src/ontology_core/blob.py packages/api/tests/test_blob.py docker-compose.yml
git commit -m "$(cat <<'EOF'
feat(core): 正本TTLのBlobクライアントを追加

レイアウトは <prefix><namespace>/<version>.ttl。Fusekiのローダが同じ
レイアウトを前提に名前空間ごとのデータセットを組み立てるため、変更時は
両方を直す必要がある旨をdocstringに明記した。

テストはモックではなくAzuriteに対して実際に読み書きする。SDKの使い方の
誤りはモックでは出ないため。composeにazuriteを追加した。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: 射影ループ(正本 → Fuseki)

Phase 1 の核心。バージョンを投入すると、正本に書いてからストアへ射影する。

**Files:**
- Create: `packages/api/src/ontology_api/repositories/versions.py`
- Create: `packages/api/src/ontology_api/services/__init__.py`
- Create: `packages/api/src/ontology_api/services/projection.py`
- Create: `packages/api/src/ontology_api/routers/versions.py`
- Create: `packages/api/tests/test_projection.py`
- Modify: `packages/api/src/ontology_api/main.py`
- Modify: `packages/api/src/ontology_api/dependencies.py`

**Interfaces:**
- Consumes: `NamespaceRepository`(Task 4)、`OntologyBlobStore`(Task 5)、`SparqlStore`(既存)、`version_graph_iri`(Task 1)
- Produces:
  - `VersionRepository(session)`
    - `async def record(self, *, namespace, version, content_hash, graph_iri, blob_path, created_by, status) -> OntologyVersion`
    - `async def find_by_hash(self, namespace: str, content_hash: str) -> OntologyVersion | None`
    - `async def list_for(self, namespace: str) -> list[OntologyVersion]`
    - `async def mark_projected(self, namespace: str, version: str) -> None`
    - `async def unprojected(self) -> list[OntologyVersion]`
  - `AuditRepository(session).record(...) -> None`
  - `ProjectionService(...)`
    - `async def publish(self, *, namespace, turtle, actor, version=None) -> OntologyVersion`
    - `async def reconcile(self) -> ReconcileReport`
  - `ReconcileReport`(dataclass: `datasets_created: list[str]`, `versions_projected: list[str]`, `failures: list[str]`)

- [ ] **Step 1: 失敗するテストを書く**

```python
# packages/api/tests/test_projection.py
"""射影ループ。正本に書いてからストアへ射影することを検証する。"""

from __future__ import annotations

import hashlib

import pytest
from ontology_core.sparql.client import SparqlStore, SparqlStoreError
from sqlalchemy.ext.asyncio import AsyncSession

from ontology_api.repositories.namespaces import NamespaceRepository
from ontology_api.repositories.versions import VersionRepository
from ontology_api.services.projection import ProjectionService

pytestmark = pytest.mark.integration

TTL = "@prefix ex: <https://e.example/#> .\nex:A a ex:Class .\n"


class FakeStore(SparqlStore):
    """射影先の代役。put_graph の呼び出しを記録する。"""

    def __init__(self, *, fail_put: bool = False) -> None:
        self.datasets: list[str] = []
        self.graphs: dict[tuple[str, str], str] = {}
        self._fail_put = fail_put

    async def query(self, sparql: str, *, dataset: str) -> dict:  # type: ignore[type-arg]
        return {"results": {"bindings": []}}

    async def update(self, sparql: str, *, dataset: str) -> None:
        return None

    async def put_graph(self, graph_iri: str, turtle: str, *, dataset: str) -> None:
        if self._fail_put:
            raise SparqlStoreError("射影に失敗しました(テスト)")
        self.graphs[(dataset, graph_iri)] = turtle

    async def list_datasets(self) -> list[str]:
        return list(self.datasets)

    async def create_dataset(self, dataset: str) -> None:
        self.datasets.append(dataset)

    async def delete_dataset(self, dataset: str) -> None:
        self.datasets.remove(dataset)


@pytest.fixture
async def prepared(session: AsyncSession, blob_store):  # noqa: ANN001, ANN201
    await NamespaceRepository(session).create(
        name="retail-core",
        display_name="小売",
        description="",
        base_iri="https://e.example/retail#",
        created_by="t",
    )
    await session.commit()
    return session, blob_store


async def test_publish_writes_source_of_truth_then_projects(prepared) -> None:  # noqa: ANN001
    session, blob = prepared
    store = FakeStore()
    svc = ProjectionService(
        session=session, blob=blob, store=store, graph_iri_base="urn:ontology:graph"
    )

    version = await svc.publish(namespace="retail-core", turtle=TTL, actor="tester")

    # 正本
    assert version.content_hash == hashlib.sha256(TTL.encode()).hexdigest()
    assert version.blob_path == "approved/retail-core/1.0.0.ttl"
    assert await blob.get_version(version.blob_path) == TTL
    # 射影
    assert ("retail-core", version.graph_iri) in store.graphs
    assert store.graphs[("retail-core", version.graph_iri)] == TTL
    # 射影済みが記録される
    rows = await VersionRepository(session).list_for("retail-core")
    assert rows[0].projected_at is not None


async def test_publish_is_idempotent_for_same_content(prepared) -> None:  # noqa: ANN001
    session, blob = prepared
    svc = ProjectionService(
        session=session, blob=blob, store=FakeStore(), graph_iri_base="urn:ontology:graph"
    )

    first = await svc.publish(namespace="retail-core", turtle=TTL, actor="t")
    second = await svc.publish(namespace="retail-core", turtle=TTL, actor="t")

    assert first.version == second.version
    assert len(await VersionRepository(session).list_for("retail-core")) == 1


async def test_version_is_incremented_for_new_content(prepared) -> None:  # noqa: ANN001
    session, blob = prepared
    svc = ProjectionService(
        session=session, blob=blob, store=FakeStore(), graph_iri_base="urn:ontology:graph"
    )

    first = await svc.publish(namespace="retail-core", turtle=TTL, actor="t")
    second = await svc.publish(
        namespace="retail-core", turtle=TTL + "\nex:B a ex:Class .\n", actor="t"
    )

    assert first.version == "1.0.0"
    assert second.version == "1.1.0"


async def test_projection_failure_keeps_source_of_truth(prepared) -> None:  # noqa: ANN001
    """射影が失敗しても正本は残り、未射影として記録される。"""
    session, blob = prepared
    svc = ProjectionService(
        session=session,
        blob=blob,
        store=FakeStore(fail_put=True),
        graph_iri_base="urn:ontology:graph",
    )

    version = await svc.publish(namespace="retail-core", turtle=TTL, actor="t")

    assert await blob.get_version(version.blob_path) == TTL
    rows = await VersionRepository(session).list_for("retail-core")
    assert rows[0].projected_at is None
    assert len(await VersionRepository(session).unprojected()) == 1


async def test_reconcile_projects_unprojected_versions(prepared) -> None:  # noqa: ANN001
    """reconcile が未射影のバージョンとデータセットを埋める。"""
    session, blob = prepared
    failing = FakeStore(fail_put=True)
    await ProjectionService(
        session=session, blob=blob, store=failing, graph_iri_base="urn:ontology:graph"
    ).publish(namespace="retail-core", turtle=TTL, actor="t")
    await session.commit()

    healthy = FakeStore()
    report = await ProjectionService(
        session=session, blob=blob, store=healthy, graph_iri_base="urn:ontology:graph"
    ).reconcile()

    assert "retail-core" in report.datasets_created
    assert len(report.versions_projected) == 1
    assert not report.failures
    assert len(await VersionRepository(session).unprojected()) == 0


async def test_audit_event_is_recorded(prepared) -> None:  # noqa: ANN001
    from sqlalchemy import select
    from ontology_core.db import AuditEventRow

    session, blob = prepared
    svc = ProjectionService(
        session=session, blob=blob, store=FakeStore(), graph_iri_base="urn:ontology:graph"
    )
    await svc.publish(namespace="retail-core", turtle=TTL, actor="alice")

    events = (await session.execute(select(AuditEventRow))).scalars().all()
    assert [e.action for e in events] == ["published"]
    assert events[0].actor == "alice"
```

`conftest.py` に `blob_store` フィクスチャを追加する(Task 5 の `store` フィクスチャを
`blob_store` に改名して共有する)。

- [ ] **Step 2: テストが失敗することを確認**

Run: `uv run pytest packages/api/tests/test_projection.py -v`
Expected: FAIL — `ModuleNotFoundError: ontology_api.services.projection`

- [ ] **Step 3: バージョンリポジトリを実装する**

```python
# packages/api/src/ontology_api/repositories/versions.py
"""オントロジーのバージョンと監査イベントの永続化。"""

from __future__ import annotations

from datetime import UTC, datetime

from ontology_core.db import AuditEventRow, OntologyVersionRow
from ontology_core.models import OntologyVersion, OntologyVersionStatus
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

__all__ = ["AuditRepository", "VersionRepository"]


def _to_model(row: OntologyVersionRow) -> OntologyVersion:
    return OntologyVersion(
        namespace=row.namespace,
        version=row.version,
        content_hash=row.content_hash,
        status=OntologyVersionStatus(row.status),
        graph_iri=row.graph_iri,
        blob_path=row.blob_path,
        created_at=row.created_at,
        created_by=row.created_by,
        approved_at=row.approved_at,
        approved_by=row.approved_by,
        projected_at=row.projected_at,
    )


class VersionRepository:
    """`ontology_versions` へのアクセス。"""

    def __init__(self, session: AsyncSession) -> None:
        self._session = session

    async def record(
        self,
        *,
        namespace: str,
        version: str,
        content_hash: str,
        graph_iri: str,
        blob_path: str,
        created_by: str,
        status: OntologyVersionStatus,
    ) -> OntologyVersion:
        row = OntologyVersionRow(
            namespace=namespace,
            version=version,
            content_hash=content_hash,
            graph_iri=graph_iri,
            blob_path=blob_path,
            created_by=created_by,
            status=status.value,
        )
        self._session.add(row)
        await self._session.flush()
        await self._session.refresh(row)
        return _to_model(row)

    async def find_by_hash(self, namespace: str, content_hash: str) -> OntologyVersion | None:
        stmt = select(OntologyVersionRow).where(
            OntologyVersionRow.namespace == namespace,
            OntologyVersionRow.content_hash == content_hash,
        )
        row = (await self._session.execute(stmt)).scalar_one_or_none()
        return _to_model(row) if row is not None else None

    async def list_for(self, namespace: str) -> list[OntologyVersion]:
        stmt = (
            select(OntologyVersionRow)
            .where(OntologyVersionRow.namespace == namespace)
            .order_by(OntologyVersionRow.id)
        )
        return [_to_model(r) for r in (await self._session.execute(stmt)).scalars()]

    async def latest_for(self, namespace: str) -> OntologyVersion | None:
        stmt = (
            select(OntologyVersionRow)
            .where(OntologyVersionRow.namespace == namespace)
            .order_by(OntologyVersionRow.id.desc())
            .limit(1)
        )
        row = (await self._session.execute(stmt)).scalar_one_or_none()
        return _to_model(row) if row is not None else None

    async def mark_projected(self, namespace: str, version: str) -> None:
        stmt = select(OntologyVersionRow).where(
            OntologyVersionRow.namespace == namespace,
            OntologyVersionRow.version == version,
        )
        row = (await self._session.execute(stmt)).scalar_one()
        row.projected_at = datetime.now(UTC)
        await self._session.flush()

    async def unprojected(self) -> list[OntologyVersion]:
        """まだ射影されていないバージョン。reconcile の対象。"""
        stmt = select(OntologyVersionRow).where(OntologyVersionRow.projected_at.is_(None))
        return [_to_model(r) for r in (await self._session.execute(stmt)).scalars()]


class AuditRepository:
    """`audit_events` へのアクセス。"""

    def __init__(self, session: AsyncSession) -> None:
        self._session = session

    async def record(
        self, *, namespace: str, action: str, actor: str, subject: str, reason: str = ""
    ) -> None:
        self._session.add(
            AuditEventRow(
                namespace=namespace,
                action=action,
                actor=actor,
                subject=subject,
                reason=reason,
            )
        )
        await self._session.flush()
```

`packages/core/src/ontology_core/models.py` の `OntologyVersion` に
`projected_at: datetime | None = None` を追加する。

- [ ] **Step 4: 射影サービスを実装する**

```python
# packages/api/src/ontology_api/services/projection.py
"""射影ループ。

**書き込み順序は「正本 → 射影」で固定する。**
1. TTL を Blob に置く(正本)
2. バージョンと監査イベントを PostgreSQL に記録する(正本)
3. Fuseki の名前付きグラフへ射影する

3 が失敗しても 1・2 は巻き戻さない。`projected_at` が NULL のまま残り、
`reconcile()` が後から埋める。逆順にすると「ストアには居るが正本に無い」
データが生まれ、レプリカ再作成で消えるため許容できない
(docs/adr/0002-triple-store-as-rebuildable-projection.md)。
"""

from __future__ import annotations

import hashlib
import logging
from dataclasses import dataclass, field

from ontology_core.blob import OntologyBlobStore
from ontology_core.graphs import dataset_name, version_graph_iri
from ontology_core.models import OntologyVersion, OntologyVersionStatus
from ontology_core.sparql.client import SparqlStore, SparqlStoreError
from sqlalchemy.ext.asyncio import AsyncSession

from ontology_api.repositories.namespaces import NamespaceRepository
from ontology_api.repositories.versions import AuditRepository, VersionRepository

logger = logging.getLogger(__name__)

__all__ = ["ProjectionService", "ReconcileReport", "UnknownNamespaceError"]


class UnknownNamespaceError(Exception):
    """存在しない名前空間を指定したことを表す。"""


@dataclass
class ReconcileReport:
    """reconcile の結果。"""

    datasets_created: list[str] = field(default_factory=list)
    versions_projected: list[str] = field(default_factory=list)
    failures: list[str] = field(default_factory=list)


def _next_version(previous: OntologyVersion | None) -> str:
    """次のバージョンを決める。

    Phase 1 では単純に minor を上げる。意味のあるバージョン付け
    (破壊的変更の検出による major 上げ)は Phase 2 の差分検出とあわせて行う。
    """
    if previous is None:
        return "1.0.0"
    major, minor, _patch = (previous.version.split(".") + ["0", "0"])[:3]
    return f"{major}.{int(minor) + 1}.0"


class ProjectionService:
    """正本への書き込みとストアへの射影を担う。"""

    def __init__(
        self,
        *,
        session: AsyncSession,
        blob: OntologyBlobStore,
        store: SparqlStore,
        graph_iri_base: str,
    ) -> None:
        self._session = session
        self._blob = blob
        self._store = store
        self._base = graph_iri_base

    async def publish(
        self, *, namespace: str, turtle: str, actor: str, version: str | None = None
    ) -> OntologyVersion:
        """オントロジーを新しいバージョンとして公開する。

        同一内容(content_hash が一致)の再投入は既存のバージョンを返す(冪等)。
        """
        namespaces = NamespaceRepository(self._session)
        if await namespaces.get(namespace) is None:
            raise UnknownNamespaceError(f"名前空間 '{namespace}' が見つかりません")

        versions = VersionRepository(self._session)
        content_hash = hashlib.sha256(turtle.encode("utf-8")).hexdigest()
        if (existing := await versions.find_by_hash(namespace, content_hash)) is not None:
            return existing

        resolved = version or _next_version(await versions.latest_for(namespace))
        graph_iri = version_graph_iri(self._base, namespace, resolved)

        # ---- 1. 正本(Blob) ----
        blob_path = await self._blob.put_version(namespace, resolved, turtle)

        # ---- 2. 正本(PostgreSQL) ----
        recorded = await versions.record(
            namespace=namespace,
            version=resolved,
            content_hash=content_hash,
            graph_iri=graph_iri,
            blob_path=blob_path,
            created_by=actor,
            status=OntologyVersionStatus.APPROVED,
        )
        await AuditRepository(self._session).record(
            namespace=namespace,
            action="published",
            actor=actor,
            subject=f"{namespace}@{resolved}",
        )

        # ---- 3. 射影 ----
        try:
            await self._store.put_graph(graph_iri, turtle, dataset=dataset_name(namespace))
            await versions.mark_projected(namespace, resolved)
        except SparqlStoreError:
            logger.exception(
                "名前空間 '%s' バージョン '%s' の射影に失敗しました。"
                "正本は保存済みです。reconcile で回復します",
                namespace,
                resolved,
            )
            return recorded

        return await versions.find_by_hash(namespace, content_hash) or recorded

    async def reconcile(self) -> ReconcileReport:
        """正本を基準にストアの状態を揃える。

        レプリカ再作成後や射影失敗後の回復に使う。
        """
        report = ReconcileReport()
        namespaces = await NamespaceRepository(self._session).list_all()
        existing = set(await self._store.list_datasets())

        for ns in namespaces:
            dataset = dataset_name(ns.name)
            if dataset not in existing:
                try:
                    await self._store.create_dataset(dataset)
                    report.datasets_created.append(ns.name)
                except SparqlStoreError as exc:
                    report.failures.append(f"{ns.name}: データセット作成に失敗 ({exc})")
                    continue

        versions = VersionRepository(self._session)
        for version in await versions.unprojected():
            try:
                turtle = await self._blob.get_version(version.blob_path)
                await self._store.put_graph(
                    version.graph_iri, turtle, dataset=dataset_name(version.namespace)
                )
                await versions.mark_projected(version.namespace, version.version)
                report.versions_projected.append(f"{version.namespace}@{version.version}")
            except (SparqlStoreError, OSError) as exc:
                report.failures.append(f"{version.namespace}@{version.version}: {exc}")

        return report
```

```python
# packages/api/src/ontology_api/services/__init__.py
"""ドメインサービス。"""

from ontology_api.services.projection import (
    ProjectionService,
    ReconcileReport,
    UnknownNamespaceError,
)

__all__ = ["ProjectionService", "ReconcileReport", "UnknownNamespaceError"]
```

- [ ] **Step 5: テストが通ることを確認**

Run: `uv run pytest packages/api/tests/test_projection.py -v`
Expected: PASS(6 件)

- [ ] **Step 6: API を公開する**

`packages/api/src/ontology_api/dependencies.py` に Blob と射影サービスの依存を追加する。

```python
async def blob_store(settings: SettingsDep) -> AsyncIterator[OntologyBlobStore]:
    from azure.identity.aio import DefaultAzureCredential

    credential = DefaultAzureCredential()
    store = OntologyBlobStore.from_account_url(
        settings.azure_storage_account_url,
        container=settings.ontology_blob_container,
        prefix=settings.ontology_blob_prefix,
        credential=credential,
    )
    try:
        yield store
    finally:
        await store.aclose()
        await credential.close()


BlobDep = Annotated[OntologyBlobStore, Depends(blob_store)]
```

`packages/api/src/ontology_api/routers/versions.py` を作る。

```python
"""オントロジーのバージョンの投入と一覧。"""

from __future__ import annotations

from fastapi import APIRouter, HTTPException, status
from ontology_core.graphs import NamespaceNameError
from ontology_core.models import OntologyVersion
from pydantic import BaseModel, Field

from ontology_api.dependencies import BlobDep, CurrentPrincipal, SessionDep, SettingsDep, StoreDep
from ontology_api.repositories.versions import VersionRepository
from ontology_api.services.projection import (
    ProjectionService,
    ReconcileReport,
    UnknownNamespaceError,
)

router = APIRouter(tags=["versions"])


class PublishRequest(BaseModel):
    """オントロジーの投入要求。"""

    turtle: str = Field(min_length=1, max_length=20_000_000, description="Turtle 形式の本文")
    version: str | None = Field(default=None, description="省略時は自動採番")


@router.post(
    "/namespaces/{namespace}/versions",
    status_code=status.HTTP_201_CREATED,
    summary="オントロジーを新しいバージョンとして公開する",
)
async def publish_version(
    namespace: str,
    payload: PublishRequest,
    principal: CurrentPrincipal,
    session: SessionDep,
    blob: BlobDep,
    store: StoreDep,
    settings: SettingsDep,
) -> OntologyVersion:
    """正本に書いてからストアへ射影する。"""
    service = ProjectionService(
        session=session, blob=blob, store=store, graph_iri_base=settings.graph_iri_base
    )
    try:
        return await service.publish(
            namespace=namespace,
            turtle=payload.turtle,
            actor=principal.object_id or principal.subject,
            version=payload.version,
        )
    except UnknownNamespaceError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    except NamespaceNameError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc


@router.get("/namespaces/{namespace}/versions", summary="バージョンの一覧を取得する")
async def list_versions(
    namespace: str, principal: CurrentPrincipal, session: SessionDep
) -> list[OntologyVersion]:
    del principal
    return await VersionRepository(session).list_for(namespace)


@router.post("/admin/reconcile", summary="正本を基準にストアの状態を揃える")
async def reconcile(
    principal: CurrentPrincipal,
    session: SessionDep,
    blob: BlobDep,
    store: StoreDep,
    settings: SettingsDep,
) -> ReconcileReport:
    """レプリカ再作成後や射影失敗後の回復に使う。

    Phase 2 で platform-admin ロールを要求するようにする。
    """
    del principal
    service = ProjectionService(
        session=session, blob=blob, store=store, graph_iri_base=settings.graph_iri_base
    )
    return await service.reconcile()
```

`main.py` に `app.include_router(versions.router)` を追加する。

- [ ] **Step 7: 全テストと lint**

Run: `uv run pytest && uv run ruff check . && uv run mypy packages`
Expected: すべて成功

- [ ] **Step 8: コミット**

```bash
git add packages/api packages/core/src/ontology_core/models.py
git commit -m "$(cat <<'EOF'
feat(api): 射影ループ(正本→ストア)とreconcileを実装

Phase 1 の核心。バージョン投入時に「Blobに置く → PostgreSQLに記録する →
Fusekiへ射影する」の順で処理する。順序を逆にすると「ストアには居るが
正本に無い」データが生まれ、レプリカ再作成で消えるため許容できない。

射影が失敗しても正本は巻き戻さず、projected_at が NULL のまま残る。
reconcile が未射影のバージョンとデータセットを後から埋める。
同一content_hashの再投入は既存バージョンを返す(冪等)。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 名前空間の隔離を実証する

ADR-0001 の「データセット単位の物理分離は検証可能」という主張を、実際に検証する。
実装が正しいと主張するだけでは不十分で、**別の名前空間のデータに到達できないことを
テストで示す**。

**Files:**
- Create: `packages/api/tests/test_isolation.py`
- Modify: `containers/fuseki/load-snapshot.sh`
- Modify: `containers/fuseki/config.ttl`

**Interfaces:**
- Consumes: 実物の Fuseki(compose)

- [ ] **Step 1: ローダが名前空間ごとに assembler と TDB2 を作るようにする**

`containers/fuseki/load-snapshot.sh` の `build_tdb` を、Blob レイアウト
`<prefix><namespace>/<version>.ttl` に従って**名前空間ごとに**処理する形へ書き換える。

生成先は次の 2 つ。
- TDB2: `${FUSEKI_BASE}/databases/<namespace>`
- assembler: `${FUSEKI_BASE}/configuration/<namespace>.ttl`

Fuseki は起動時に `FUSEKI_BASE/configuration/*.ttl` を読み込むため、これで
名前空間ごとのデータセットが立ち上がる(Jena 公式のディレクトリ規約)。

assembler の雛形(`<namespace>` と `<location>` を置換して書き出す)。

```turtle
@prefix fuseki: <http://jena.apache.org/fuseki#> .
@prefix ja:     <http://jena.hpl.hp.com/2005/11/Assembler#> .
@prefix rdf:    <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix tdb2:   <http://jena.apache.org/2016/tdb#> .

<#service> rdf:type fuseki:Service ;
    fuseki:name "NAMESPACE" ;
    fuseki:endpoint [ fuseki:operation fuseki:query  ; fuseki:name "sparql" ] ;
    fuseki:endpoint [ fuseki:operation fuseki:query  ; fuseki:name "query"  ] ;
    fuseki:endpoint [ fuseki:operation fuseki:update ; fuseki:name "update" ] ;
    fuseki:endpoint [ fuseki:operation fuseki:gsp-rw ; fuseki:name "data"   ] ;
    fuseki:dataset <#dataset> .

<#dataset> rdf:type tdb2:DatasetTDB2 ;
    tdb2:location "LOCATION" ;
    ja:context [ ja:cxtName "arq:queryTimeout"  ; ja:cxtValue "10000,30000" ] ;
    ja:context [ ja:cxtName "arq:updateTimeout" ; ja:cxtValue "20000,60000" ] ;
    ja:context [ ja:cxtName "tdb2:unionDefaultGraph" ; ja:cxtValue true ] .
```

**注意:** `SERVICE` の無効化(`arq:httpServiceAllowed false`)は `config.ttl` の
`fuseki:Server` レベルで設定しており、サーバー全体に効く。名前空間ごとの assembler に
書く必要はないが、**Task 7 Step 4 で動的に作ったデータセットでも 422 になることを
必ず検証する**こと。効いていなければ assembler 側にも入れる。

- [ ] **Step 2: 隔離テストを書く**

```python
# packages/api/tests/test_isolation.py
"""名前空間の隔離を実物の Fuseki に対して検証する。

ADR-0001 は「データセット単位の物理分離は検証可能」と述べている。
その主張をここで実際に確かめる。
"""

from __future__ import annotations

import os

import pytest
from ontology_core.sparql.client import FusekiStore, SparqlStoreError

pytestmark = pytest.mark.integration

_PORT = os.environ.get("FUSEKI_PORT", "3030")
_BASE = f"http://localhost:{_PORT}"


@pytest.fixture
async def store() -> FusekiStore:
    s = FusekiStore(
        query_endpoint=_BASE + "/{dataset}/sparql",
        update_endpoint=_BASE + "/{dataset}/update",
        gsp_endpoint=_BASE + "/{dataset}/data",
        admin_endpoint=_BASE + "/$/",
        admin_auth=("admin", os.environ.get("FUSEKI_ADMIN_PASSWORD", "localdev")),
    )
    yield s
    await s.aclose()


async def _count(store: FusekiStore, dataset: str) -> int:
    result = await store.query("SELECT (COUNT(*) AS ?n) WHERE { ?s ?p ?o }", dataset=dataset)
    return int(result["results"]["bindings"][0]["n"]["value"])


async def test_data_in_one_namespace_is_invisible_from_another(store: FusekiStore) -> None:
    for ns in ("iso-alpha", "iso-beta"):
        if ns not in await store.list_datasets():
            await store.create_dataset(ns)

    await store.put_graph(
        "urn:ontology:graph/iso-alpha/1.0.0",
        "@prefix ex: <https://e.example/#> .\nex:Secret a ex:Class .\n",
        dataset="iso-alpha",
    )

    assert await _count(store, "iso-alpha") == 1
    # beta 側からは 1 件も見えないこと。
    assert await _count(store, "iso-beta") == 0

    # beta で alpha のグラフを名指ししても取れないこと。
    result = await store.query(
        "SELECT ?s WHERE { GRAPH <urn:ontology:graph/iso-alpha/1.0.0> { ?s ?p ?o } }",
        dataset="iso-beta",
    )
    assert result["results"]["bindings"] == []

    await store.delete_dataset("iso-alpha")
    await store.delete_dataset("iso-beta")


async def test_service_clause_is_blocked_on_dynamic_dataset(store: FusekiStore) -> None:
    """動的に作ったデータセットでも SERVICE がブロックされること。

    SERVICE の無効化はサーバーレベルの設定なので、admin API で作った
    データセットにも効くはず。効いていなければ SSRF の穴になる。
    """
    dataset = "iso-service"
    if dataset not in await store.list_datasets():
        await store.create_dataset(dataset)

    with pytest.raises(SparqlStoreError) as exc:
        await store.query(
            "SELECT ?s WHERE { SERVICE <http://169.254.169.254/> { ?s ?p ?o } }",
            dataset=dataset,
        )
    assert "422" in str(exc.value) or "disabled" in str(exc.value)

    await store.delete_dataset(dataset)
```

- [ ] **Step 3: テストを実行して失敗を確認**

Run: `docker compose up -d --build fuseki && uv run pytest packages/api/tests/test_isolation.py -v`
Expected: 隔離テストは PASS するはず。**SERVICE のテストが FAIL したら**、
assembler 雛形に `ja:context [ ja:cxtName "arq:httpServiceAllowed" ; ja:cxtValue false ]` を
追加して再実行する

- [ ] **Step 4: 結果に応じて config を修正し、再実行**

Run: `docker compose up -d --build fuseki && uv run pytest packages/api/tests/test_isolation.py -v`
Expected: PASS(2 件)

- [ ] **Step 5: ローダの変更をローカルで通す**

Run:
```bash
docker compose down -v && docker compose up -d --build
sleep 40
docker compose logs fuseki | grep -E "entrypoint:|load-snapshot:"
```
Expected: サンプルが `retail-core` データセットとして読み込まれている

- [ ] **Step 6: shellcheck と全テスト**

Run:
```bash
docker run --rm -v "$(pwd):/mnt" koalaman/shellcheck:stable /mnt/containers/fuseki/*.sh
uv run pytest && uv run ruff check . && uv run mypy packages
```
Expected: すべて成功

- [ ] **Step 7: コミット**

```bash
git add containers/fuseki packages/api/tests/test_isolation.py
git commit -m "$(cat <<'EOF'
feat(fuseki): 名前空間ごとのデータセットを生成し隔離を実証する

ローダがBlobレイアウト <prefix><namespace>/<version>.ttl に従って
名前空間ごとにTDB2とassemblerを生成する。Fusekiは起動時に
FUSEKI_BASE/configuration/*.ttl を読み込むため、これで名前空間ごとの
データセットが立ち上がる。

ADR-0001の「データセット単位の物理分離は検証可能」という主張を、
実物のFusekiに対するテストで実際に確認した。別の名前空間のグラフを
名指ししても0件であること、動的に作ったデータセットでもSERVICEが
ブロックされることを検証している。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Bicep と postprovision を更新し、Azure で検証する

**Files:**
- Modify: `infra/modules/postgres.bicep`
- Modify: `infra/modules/api.bicep`
- Modify: `infra/main.bicep`
- Create: `scripts/postprovision.sh`
- Create: `scripts/postprovision.ps1`
- Modify: `azure.yaml`
- Modify: `README.md`

- [ ] **Step 1: DB マイグレーションの適用方法を決めて実装する**

API コンテナの起動時に `alembic upgrade head` を実行する。複数レプリカが同時に
起動してもマイグレーションは Alembic のロックで直列化される。

`packages/api/Dockerfile` の `CMD` を起動スクリプト経由に変える。

```dockerfile
COPY --chown=appuser:appuser packages/api/docker-entrypoint.sh /app/docker-entrypoint.sh
CMD ["/app/docker-entrypoint.sh"]
```

```sh
#!/bin/sh
# API の起動ラッパー。マイグレーションを適用してからサーバーを起動する。
set -eu
echo "entrypoint: DB マイグレーションを適用します" >&2
cd /app/packages/api && alembic upgrade head
cd /app
exec uvicorn ontology_api.main:app --host 0.0.0.0 --port 8000
```

- [ ] **Step 2: 新しい環境変数を Bicep に追加する**

`api.bicep` の env に `GRAPH_IRI_BASE` と `BLOB_PREFIX` を追加する
(`fuseki.bicep` の `graphIriBase` と同じ値を `main.bicep` から渡す)。

- [ ] **Step 3: PostgreSQL のロールを用意する**

マネージド ID が PostgreSQL に接続できるよう、`postgres.bicep` の Entra 管理者設定を
確認し、`ontology` データベースへの権限付与手順を README に書く
(Entra ロールの作成は SQL 実行が必要なため IaC だけでは完結しない)。

- [ ] **Step 4: postprovision スクリプトを実装する**

サンプルオントロジーを Blob へ置き、名前空間を作って射影する。

```sh
#!/bin/sh
# azd provision 後にサンプルオントロジーを投入する。
set -eu
: "${AZURE_STORAGE_ACCOUNT_NAME:?}"
echo "postprovision: サンプルオントロジーを Blob へ配置します"
az storage blob upload \
  --account-name "${AZURE_STORAGE_ACCOUNT_NAME}" \
  --container-name "${ONTOLOGY_BLOB_CONTAINER:-ontologies}" \
  --name "approved/retail-core/1.0.0.ttl" \
  --file "samples/retail-core.ttl" \
  --auth-mode login --overwrite --only-show-errors
echo "postprovision: 完了しました"
```

`azure.yaml` の `postprovision` をこのスクリプトに差し替える(`continueOnError: true` は維持)。

- [ ] **Step 5: Azure へデプロイして検証する**

Run: `azd up --no-prompt`
Expected: SUCCESS

検証すること。
1. API `/healthz` が 200
2. `POST /admin/reconcile` が名前空間とバージョンを揃える(要トークン。Task 9 と併せて実施)
3. Fuseki に `retail-core` データセットが存在し、SPARQL が 60 トリプルを返す
   (API コンテナ内から `az containerapp exec` で内部 FQDN 経由に確認)
4. 別の名前空間を作り、隔離されていること

- [ ] **Step 6: 検証後にリソースを削除する**

Run: `azd down --purge --force --no-prompt`

- [ ] **Step 7: README を更新してコミット**

手動アップロード手順を postprovision で自動化した旨に書き換え、
Azure 実環境での検証結果を追記する。

---

## 後続タスク(本計画の範囲外・Phase 1 の残り)

コア機能が終わったら次に着手する。それぞれ独立して進められる。

| # | 内容 | 備考 |
| --- | --- | --- |
| 9 | Entra ID App 登録と認証経路の実証 | 利用者は登録可能と回答済み。有効なトークンで API が通ることまで確認する |
| 10 | MCP から Core API へのトークン伝播 | On-Behalf-Of か、エージェント自身のトークンを転送する方式を決める |
| 11 | スパイク①起動時再構築の所要時間実測 | 名前空間数とトリプル数を変えて計測。ADR-0002 の残論点 |
| 12 | スパイク②ACA の idle/active 課金比率の実測 | 月$51〜61 の試算が最悪$105まで上振れするかを確定 |
| 13 | スパイク③Ontop 配布物のライセンス確認 | Phase 3 の前提 |

---

## Self-Review

**1. Spec coverage**

| Spec の要求 | 対応タスク |
| --- | --- |
| 正本は PostgreSQL(ADR-0003) | Task 3, 4, 6 |
| 正本の TTL は Blob(ADR-0002/0006) | Task 5, 6 |
| 名前空間はデータセット単位で隔離(ADR-0001) | Task 4(作成)、Task 7(生成と実証) |
| 書き込み口は Core API のみ、順序は正本→射影(ADR-0002) | Task 6 |
| 再構築可能な射影(ADR-0002) | Task 7(ローダ)、Task 6(reconcile) |
| 不変リビジョンと監査証跡(ADR-0006) | Task 3(テーブル)、Task 6(記録) |
| SPARQL 攻撃面対策 | 既存。Task 7 で動的データセットでも有効か検証 |
| Entra JWT 検証 | 既存(401 まで)。後続タスク 9 で実証 |
| Apache-2.0 互換のライセンスのみ | Task 2 Step 6(psycopg → asyncpg) |

**2. Placeholder scan** — 「TBD」「後で実装」「適切なエラー処理」の類は無し。
Task 8 Step 3 のみ「README に書く」という記述だが、これは IaC で完結しない
事実(Entra ロール作成に SQL 実行が必要)を明示したものであり、
実施内容は特定できている。

**3. Type consistency** — 確認した点。
- `dataset_name` / `version_graph_iri` / `validate_namespace_name` / `validate_version` は
  Task 1 で定義し、Task 4・5・6・7 で同名で使用
- `OntologyVersion` に `projected_at` を追加(Task 6 Step 3)。Task 6 のテストが参照
- `SparqlStore` の抽象メソッド 6 つは既存の署名どおり。Task 6 の `FakeStore` が全て実装
- `ReconcileReport` のフィールド名(`datasets_created` / `versions_projected` / `failures`)は
  Task 6 のテストと実装で一致
- `conftest.py` の `blob_store` フィクスチャ名は Task 5 で `store` として作り
  Task 6 Step 1 で改名する旨を明記
